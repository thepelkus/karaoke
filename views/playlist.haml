%p
  %ul
    - songs.each_with_index do |song, index|
      %li
        - delete_url = song[:current] ? "/skip_current" : "/delete_from_playlist/#{index}"
        %span{style: song[:current] ? "font-weight: bold" : "" } #{song[:name]}
        %a(href=delete_url) [x]
